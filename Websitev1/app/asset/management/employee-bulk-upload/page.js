import EmployeeBulkUpload from "@/widgets/assetManagement/EmployeeBulkUpload";

export const metadata = {
  title: "Employee Bulk Upload | Lupin MIS",
  description: "Bulk upload employee records.",
};

const Page = () => {
  return <EmployeeBulkUpload />;
};

export default Page;
