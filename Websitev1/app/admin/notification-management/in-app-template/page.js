import ViewTemplates from "@/widgets/NotificationManagement/ViewTemplates";

const page = (props) => {
  return (
    <div>
        <ViewTemplates 
            mainTitle= "IN-APP NOTIFICATION Template"
            templateType= "INAPP NOTIFICATION"
        />
    </div>
  );
};
export default page;
