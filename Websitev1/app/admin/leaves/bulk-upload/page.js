import LeaveBulkUpload from "@/widgets/leaves/LeaveBulkUpload";

export const metadata = {
  title: "Leave Balance Bulk Upload | HRMS",
  description: "Bulk upload leave balance records for employees.",
};

const Page = () => {
  return <LeaveBulkUpload />;
};

export default Page;
